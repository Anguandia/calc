language: python
python:
  - "3.7-dev"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest coveralls/tests.py
after success:
  - "sbt coveralls"
notifications:
  email:
    recipients:
      - mikeanguandia@yahoo.com
